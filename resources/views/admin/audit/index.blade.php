@extends('admin.layouts.app')
@section('title','Audit') @section('header','Audit Log') @section('content')@if(!empty($setupRequired))<div class="mb-6 border border-amber-300 bg-amber-50 p-5 text-sm"><b>Module setup required.</b> Run <code class="rounded bg-white px-1 py-0.5">php artisan migrate</code>, then refresh this page. The admin panel will no longer crash while the table is missing.</div>@endif<div class="border bg-white">@forelse($logs as $l)<div class="grid gap-1 border-b p-4 text-sm md:grid-cols-[160px_1fr_180px]"><span>{{ $l->created_at->format('d M Y H:i') }}</span><span>{{ $l->action }} · {{ $l->entity_type }} #{{ $l->entity_id }}</span><span class="text-right text-black/45">{{ $l->ip_address }}</span></div>@empty<div class="p-8 text-black/45">Audit events will appear here as administrative actions are instrumented.</div>@endforelse</div><div class="mt-6">{{ $logs->links() }}</div>@endsection