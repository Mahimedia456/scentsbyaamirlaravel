@extends('admin.layouts.app')
@section('title','Audit') @section('header','Audit Log') @section('eyebrow','Security / accountability')
@section('content')
<div style="display:flex;justify-content:space-between;align-items:flex-end;gap:14px;flex-wrap:wrap;margin-bottom:18px"><div><h2 style="font-size:27px;margin:0">Administrative audit</h2><p class="admin-muted" style="font-size:12px;margin:7px 0 0">Mutating admin actions are recorded with user, route, entity, IP and response metadata.</p></div><a class="admin-btn" href="{{ route('admin.audit.export') }}">Export audit CSV</a></div>
<section class="admin-card"><form method="GET" style="display:grid;grid-template-columns:1fr 170px 180px 150px 150px auto;gap:8px;padding:14px;border-bottom:1px solid #e4e7ec"><input class="admin-field" name="q" value="{{ request('q') }}" placeholder="Search action"><input class="admin-field" name="entity_type" value="{{ request('entity_type') }}" placeholder="Entity type"><select class="admin-field" name="user_id"><option value="">All admins</option>@foreach($users as $u)<option value="{{ $u->id }}" @selected((string)request('user_id')===(string)$u->id)>{{ $u->name }}</option>@endforeach</select><input class="admin-field" type="date" name="date_from" value="{{ request('date_from') }}"><input class="admin-field" type="date" name="date_to" value="{{ request('date_to') }}"><button class="admin-btn">Filter</button></form>
<div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Date</th><th>Action</th><th>Entity</th><th>Admin</th><th>IP</th><th>Result</th></tr></thead><tbody>@forelse($logs as $l)<tr><td>{{ $l->created_at->format('d M Y H:i:s') }}</td><td style="font-weight:650">{{ $l->action }}</td><td>{{ $l->entity_type ?: '—' }}{{ $l->entity_id?' #'.$l->entity_id:'' }}</td><td>#{{ $l->user_id ?: '—' }}</td><td>{{ $l->ip_address }}</td><td>{{ data_get($l->meta,'status','—') }}</td></tr>@empty<tr><td colspan="6" class="admin-muted" style="padding:45px;text-align:center">No audit events match these filters.</td></tr>@endforelse</tbody></table></div></section><div style="margin-top:18px">{{ $logs->links() }}</div>@endsection
