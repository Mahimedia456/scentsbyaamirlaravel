@extends('admin.layouts.app')
@section('title','Shipping') @section('header','Shipping Zones') @section('content')<form method="POST" action="{{ route('admin.shipping.store') }}" class="mb-8 grid gap-3 border bg-white p-5 md:grid-cols-3">@csrf<input name="name" placeholder="Pakistan Standard" class="border p-3"><input name="country_code" value="PK" class="border p-3"><input name="regions" placeholder="Punjab, Sindh..." class="border p-3"><input name="base_rate" type="number" step="0.01" placeholder="Base rate" class="border p-3"><input name="free_shipping_over" type="number" step="0.01" placeholder="Free over" class="border p-3"><label class="flex items-center gap-2"><input type="checkbox" name="active" checked> Active</label><button class="bg-black p-3 text-white md:col-span-3">Add zone</button></form><div class="border bg-white">@foreach($zones as $z)<div class="flex justify-between border-b p-5"><div><b>{{ $z->name }}</b><p class="text-xs text-black/45">{{ $z->country_code }} · PKR {{ number_format($z->base_rate) }} · Free over {{ $z->free_shipping_over ?: '—' }}</p></div><form method="POST" action="{{ route('admin.shipping.destroy',$z) }}">@csrf @method('DELETE')<button class="text-xs underline">Delete</button></form></div>@endforeach</div>@endsection