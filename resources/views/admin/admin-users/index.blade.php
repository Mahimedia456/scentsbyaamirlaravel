@extends('admin.layouts.app')
@section('title','Admin Users') @section('header','Admin Users & Roles') @section('content')@if(!empty($setupRequired))<div class="mb-6 border border-amber-300 bg-amber-50 p-5 text-sm"><b>Module setup required.</b> Run <code class="rounded bg-white px-1 py-0.5">php artisan migrate</code>, then refresh this page. The admin panel will no longer crash while the table is missing.</div>@endif<form method="POST" action="{{ route('admin.admin-users.store') }}" class="mb-8 grid gap-3 border bg-white p-5 md:grid-cols-4">@csrf<input name="name" placeholder="Name" class="border p-3"><input name="email" type="email" placeholder="Email" class="border p-3"><input name="password" type="password" placeholder="Temporary password" class="border p-3"><select name="role" class="border p-3"><option value="admin">Admin</option><option value="editor">Editor</option><option value="super_admin">Super admin</option></select><button class="bg-black p-3 text-white md:col-span-4">Create admin</button></form><div class="border bg-white">@foreach($users as $u)<div class="grid grid-cols-3 border-b p-4 text-sm"><b>{{ $u->name }}</b><span>{{ $u->email }}</span><span class="text-right capitalize">{{ str_replace('_',' ',$u->role) }}</span></div>@endforeach</div>@endsection