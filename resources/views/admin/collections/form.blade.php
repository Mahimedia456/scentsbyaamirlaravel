@extends('admin.layouts.app')
@section('title',$collection->exists?'Edit Collection':'New Collection')
@section('content')
<form method="POST" action="{{ $collection->exists?route('admin.collections.update',$collection):route('admin.collections.store') }}" class="mx-auto max-w-3xl space-y-6">@csrf @if($collection->exists)@method('PUT')@endif<div><p class="text-xs uppercase tracking-[.28em] text-neutral-500">Merchandising</p><h1 class="mt-2 text-3xl font-semibold">{{ $collection->exists?'Edit collection':'New collection' }}</h1></div><div class="rounded-3xl border bg-white p-6"><div class="grid gap-5"><label>Name<input name="name" value="{{ old('name',$collection->name) }}" class="mt-2 w-full rounded-2xl border-neutral-200"></label><label>Slug<input name="slug" value="{{ old('slug',$collection->slug) }}" class="mt-2 w-full rounded-2xl border-neutral-200"></label><label>Description<textarea name="description" rows="5" class="mt-2 w-full rounded-2xl border-neutral-200">{{ old('description',$collection->description) }}</textarea></label><label>Sort order<input name="sort_order" type="number" value="{{ old('sort_order',$collection->sort_order ?? 0) }}" class="mt-2 w-full rounded-2xl border-neutral-200"></label><label class="flex items-center gap-2"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$collection->exists?$collection->is_active:true))> Active</label><button class="rounded-full bg-black px-6 py-3 text-white">Save collection</button></div></div></form>
@endsection
