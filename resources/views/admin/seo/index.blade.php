@extends('admin.layouts.app')
@section('title','SEO') @section('header','SEO & Redirects') @section('content')<form method="POST" action="{{ route('admin.seo.store') }}" class="mb-8 grid gap-3 border bg-white p-5 md:grid-cols-4">@csrf<input name="from_path" placeholder="/old-path" class="border p-3"><input name="to_path" placeholder="/new-path" class="border p-3"><select name="status_code" class="border p-3"><option>301</option><option>302</option></select><label class="flex items-center gap-2"><input type="checkbox" name="active" value="1" checked> Active</label><button class="bg-black p-3 text-white md:col-span-4">Add redirect</button></form><div class="border bg-white">@foreach($redirects as $r)<div class="flex justify-between border-b p-4 text-sm"><span>{{ $r->from_path }} → {{ $r->to_path }} <small>({{ $r->status_code }})</small></span><form method="POST" action="{{ route('admin.seo.destroy',$r) }}">@csrf @method('DELETE')<button class="underline">Delete</button></form></div>@endforeach</div><div class="mt-6">{{ $redirects->links() }}</div>@endsection