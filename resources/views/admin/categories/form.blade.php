@extends('admin.layouts.app')
@section('title',$category->exists?'Edit Category':'New Category')
@section('content')
<form method="POST" action="{{ $category->exists?route('admin.categories.update',$category):route('admin.categories.store') }}" class="mx-auto max-w-3xl space-y-6">@csrf @if($category->exists)@method('PUT')@endif<div><p class="text-xs uppercase tracking-[.28em] text-neutral-500">Catalog</p><h1 class="mt-2 text-3xl font-semibold">{{ $category->exists?'Edit category':'New category' }}</h1></div><div class="rounded-3xl border bg-white p-6"><div class="grid gap-5"><label>Name<input name="name" value="{{ old('name',$category->name) }}" class="mt-2 w-full rounded-2xl border-neutral-200"></label><label>Slug<input name="slug" value="{{ old('slug',$category->slug) }}" class="mt-2 w-full rounded-2xl border-neutral-200"></label><label>Description<textarea name="description" rows="5" class="mt-2 w-full rounded-2xl border-neutral-200">{{ old('description',$category->description) }}</textarea></label><label>Sort order<input name="sort_order" type="number" value="{{ old('sort_order',$category->sort_order ?? 0) }}" class="mt-2 w-full rounded-2xl border-neutral-200"></label><label class="flex items-center gap-2"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$category->exists?$category->is_active:true))> Active</label><button class="rounded-full bg-black px-6 py-3 text-white">Save category</button></div></div></form>
@endsection
