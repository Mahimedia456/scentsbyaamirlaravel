@extends('admin.layouts.app')
@section('title','Analytics') @section('header','Analytics & Reports') @section('content')@if(!empty($setupRequired))<div class="mb-6 border border-amber-300 bg-amber-50 p-5 text-sm"><b>Module setup required.</b> Run <code class="rounded bg-white px-1 py-0.5">php artisan migrate</code>, then refresh this page. The admin panel will no longer crash while the table is missing.</div>@endif<div class="grid gap-px bg-black/10 border sm:grid-cols-2 xl:grid-cols-5">@foreach([['Revenue','PKR '.number_format($stats['revenue'])],['Orders',number_format($stats['orders'])],['Customers',number_format($stats['customers'])],['Products',number_format($stats['products'])],['AOV','PKR '.number_format($stats['aov'])]] as [$l,$v])<div class="bg-white p-6"><p class="text-xs uppercase text-black/45">{{ $l }}</p><p class="mt-4 text-2xl">{{ $v }}</p></div>@endforeach</div><div class="mt-8 border bg-white"><div class="border-b p-5 font-medium">Top products</div>@forelse($top as $p)<div class="grid grid-cols-3 border-b p-4 text-sm"><span>{{ $p->product_name }}</span><span>{{ $p->qty }} units</span><span class="text-right">PKR {{ number_format($p->revenue) }}</span></div>@empty<div class="p-8 text-sm text-black/45">No sales data yet.</div>@endforelse</div>@endsection