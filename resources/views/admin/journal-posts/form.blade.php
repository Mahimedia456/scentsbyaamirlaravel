@extends('admin.layouts.app')
@section('title',$post->exists?'Edit Article':'Create Article') @section('header',$post->exists?'Edit Article':'Create Article') @section('eyebrow','CMS / editorial')
@section('content')
<form method="POST" action="{{ $post->exists?route('admin.journal-posts.update',$post):route('admin.journal-posts.store') }}">@csrf @if($post->exists)@method('PUT')@endif<div style="display:flex;justify-content:space-between;align-items:flex-end;gap:14px;flex-wrap:wrap;margin-bottom:18px"><div><a href="{{ route('admin.journal-posts.index') }}" class="admin-muted" style="font-size:10px">← Back</a><h2 style="font-size:27px;margin:7px 0 0">{{ $post->exists?$post->title:'New article' }}</h2></div><button class="admin-btn admin-btn-primary">Save article</button></div><div style="display:grid;grid-template-columns:minmax(0,1fr) 330px;gap:14px;align-items:start"><section class="admin-card" style="padding:20px"><div class="admin-eyebrow">Editorial content</div><div style="display:grid;gap:11px;margin-top:15px"><input class="admin-field" required name="title" value="{{ old('title',$post->title) }}" placeholder="Article title"><input class="admin-field" name="slug" value="{{ old('slug',$post->slug) }}" placeholder="Slug"><input class="admin-field" name="eyebrow" value="{{ old('eyebrow',$post->eyebrow) }}" placeholder="Eyebrow / section"><textarea class="admin-field" style="padding-top:10px;min-height:100px" name="excerpt" placeholder="Excerpt">{{ old('excerpt',$post->excerpt) }}</textarea><textarea class="admin-field" style="padding-top:10px;min-height:420px;font-family:ui-monospace,monospace;font-size:11px" name="content" placeholder="Article content / HTML">{{ old('content',$post->content) }}</textarea></div></section><aside style="display:grid;gap:14px;position:sticky;top:96px"><section class="admin-card" style="padding:20px"><div class="admin-eyebrow">Publishing</div><div style="display:grid;gap:10px;margin-top:14px"><select class="admin-field" name="status">@foreach(['draft','published','archived'] as $s)<option value="{{ $s }}" @selected(old('status',$post->status?:'draft')===$s)>{{ ucfirst($s) }}</option>@endforeach</select><input class="admin-field" type="datetime-local" name="published_at" value="{{ old('published_at',optional($post->published_at)->format('Y-m-d\TH:i')) }}"><input class="admin-field" name="featured_image_path" value="{{ old('featured_image_path',$post->featured_image_path) }}" placeholder="Featured media path"></div></section><section class="admin-card" style="padding:20px"><div class="admin-eyebrow">SEO</div><div style="display:grid;gap:10px;margin-top:14px"><input class="admin-field" name="meta_title" value="{{ old('meta_title',$post->meta_title) }}" placeholder="Meta title"><textarea class="admin-field" style="padding-top:10px;min-height:100px" name="meta_description" placeholder="Meta description">{{ old('meta_description',$post->meta_description) }}</textarea></div></section></aside></div></form>@endsection
