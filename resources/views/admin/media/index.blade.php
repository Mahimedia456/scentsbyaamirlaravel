@extends('admin.layouts.app')
@section('title','Media') @section('header','Media Library') @section('content')
<form method="POST" enctype="multipart/form-data" action="{{ route('admin.media.store') }}" class="mb-8 grid gap-3 border border-black/10 bg-white p-5 md:grid-cols-[1fr_1fr_auto]">@csrf<input type="file" name="file" required class="border p-3"><input name="alt_text" placeholder="Alt text" class="border p-3"><button class="bg-black px-6 text-white">Upload</button></form><div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach($assets as $a)<article class="border bg-white p-4"><div class="aspect-square bg-black/5 overflow-hidden">@if(str_starts_with($a->mime_type??'','image/'))<img src="{{ route('store.media', ['path' => preg_replace('#^/?storage/#', '', $a->path)]) }}" class="h-full w-full object-cover">@endif</div><p class="mt-3 truncate text-sm font-medium">{{ $a->name }}</p><p class="mt-1 text-xs text-black/45">{{ number_format($a->size_bytes/1024) }} KB</p><form method="POST" action="{{ route('admin.media.destroy',$a) }}" class="mt-3">@csrf @method('DELETE')<button class="text-xs underline">Delete</button></form></article>@endforeach</div><div class="mt-6">{{ $assets->links() }}</div>@endsection