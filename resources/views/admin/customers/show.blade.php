@extends('admin.layouts.app')
@section('title',$customer->full_name) @section('header',$customer->full_name)
@section('content')
<div class="mb-6 flex justify-end"><a href="{{ route('admin.customers.edit',$customer) }}" class="bg-black px-5 py-3 text-xs uppercase tracking-[.16em] text-white">Edit customer</a></div><div class="grid gap-6 lg:grid-cols-[320px_1fr]"><aside class="border border-black/10 bg-white p-6"><p class="text-lg font-medium">{{ $customer->full_name }}</p><div class="mt-4 space-y-2 text-sm text-black/60"><p>{{ $customer->email }}</p><p>{{ $customer->phone }}</p><p>{{ $customer->company }}</p><p>{{ $customer->is_active?'Active':'Inactive' }}</p></div>@if($customer->notes)<div class="mt-6 border-t border-black/10 pt-4 text-sm">{{ $customer->notes }}</div>@endif</aside><section class="border border-black/10 bg-white p-6"><h2 class="mb-4 text-lg font-medium">Recent orders</h2>@forelse($customer->orders as $order)<a href="{{ route('admin.orders.show',$order) }}" class="flex justify-between border-t border-black/10 py-4 text-sm"><span>{{ $order->order_number }} · {{ ucfirst($order->status) }}</span><span>{{ $order->currency }} {{ number_format((float)$order->grand_total,2) }}</span></a>@empty<p class="text-sm text-black/45">No orders yet.</p>@endforelse</section></div>
@endsection
