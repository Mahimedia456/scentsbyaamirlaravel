@extends('admin.layouts.app')
@section('title',$customer->exists?'Edit Customer':'New Customer') @section('header',$customer->exists?'Edit Customer':'New Customer')
@section('content')
<form method="POST" action="{{ $customer->exists?route('admin.customers.update',$customer):route('admin.customers.store') }}" class="max-w-3xl border border-black/10 bg-white p-6 md:p-8">@csrf @if($customer->exists)@method('PUT')@endif
<div class="grid gap-4 md:grid-cols-2"><input required name="first_name" value="{{ old('first_name',$customer->first_name) }}" placeholder="First name" class="border border-black/15 p-3"><input name="last_name" value="{{ old('last_name',$customer->last_name) }}" placeholder="Last name" class="border border-black/15 p-3"><input name="email" value="{{ old('email',$customer->email) }}" placeholder="Email" class="border border-black/15 p-3"><input name="phone" value="{{ old('phone',$customer->phone) }}" placeholder="Phone" class="border border-black/15 p-3"><input name="company" value="{{ old('company',$customer->company) }}" placeholder="Company" class="border border-black/15 p-3 md:col-span-2"></div><textarea name="notes" rows="5" placeholder="Internal notes" class="mt-4 w-full border border-black/15 p-3">{{ old('notes',$customer->notes) }}</textarea><div class="mt-5 flex flex-wrap gap-6 text-sm"><label><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$customer->exists?$customer->is_active:true))> Active</label><label><input type="checkbox" name="marketing_opt_in" value="1" @checked(old('marketing_opt_in',$customer->marketing_opt_in))> Marketing opt-in</label></div><button class="mt-6 bg-black px-6 py-3 text-xs uppercase tracking-[.16em] text-white">Save customer</button></form>
@endsection
