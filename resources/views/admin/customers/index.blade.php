@extends('admin.layouts.app')
@section('title','Customers') @section('header','Customers')
@section('content')
<div class="mb-6 flex flex-col gap-3 md:flex-row md:items-center md:justify-between"><form class="flex max-w-xl flex-1 gap-2"><input name="q" value="{{ request('q') }}" placeholder="Search customers" class="min-w-0 flex-1 border border-black/15 bg-white px-4 py-3"><button class="bg-black px-5 text-xs uppercase tracking-[.16em] text-white">Search</button></form><a href="{{ route('admin.customers.create') }}" class="bg-black px-5 py-3 text-xs uppercase tracking-[.16em] text-white">New customer</a></div>
<div class="overflow-x-auto border border-black/10 bg-white"><table class="w-full text-sm"><thead class="border-b border-black/10 bg-black/[.03] text-left text-[10px] uppercase tracking-[.15em] text-black/50"><tr><th class="p-4">Customer</th><th class="p-4">Contact</th><th class="p-4">Orders</th><th class="p-4">Lifetime value</th><th class="p-4">Status</th></tr></thead><tbody>@foreach($customers as $customer)<tr class="border-b border-black/5"><td class="p-4"><a href="{{ route('admin.customers.show',$customer) }}" class="font-medium underline">{{ $customer->full_name }}</a></td><td class="p-4"><div>{{ $customer->email }}</div><div class="text-xs text-black/45">{{ $customer->phone }}</div></td><td class="p-4">{{ $customer->orders_count }}</td><td class="p-4">PKR {{ number_format((float)($customer->orders_sum_grand_total ?? 0),2) }}</td><td class="p-4">{{ $customer->is_active ? 'Active' : 'Inactive' }}</td></tr>@endforeach</tbody></table></div><div class="mt-6">{{ $customers->links() }}</div>
@endsection
