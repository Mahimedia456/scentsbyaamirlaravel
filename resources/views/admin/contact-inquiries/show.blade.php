@extends('admin.layouts.app') @section('title','Support Request') @section('header','Support Request') @section('content')
<div class="grid gap-6 lg:grid-cols-[1.2fr_.8fr]"><div class="border bg-white p-7"><p class="text-xs uppercase tracking-wider text-black/40">{{ $inquiry->subject }}</p><h2 class="mt-3 text-2xl font-medium">{{ $inquiry->first_name }} {{ $inquiry->last_name }}</h2><p class="mt-2 text-sm">{{ $inquiry->email }} @if($inquiry->order_number) · {{ $inquiry->order_number }} @endif</p><div class="mt-8 whitespace-pre-line border-t pt-6 text-sm leading-7">{{ $inquiry->message }}</div></div><form method="POST" action="{{ route('admin.contact-inquiries.update',$inquiry) }}" class="h-fit space-y-5 border bg-white p-7">@csrf @method('PUT')<label class="block"><span class="mb-2 block text-xs uppercase tracking-wider">Status</span><select name="status" class="w-full border p-3">@foreach(['new','in-progress','resolved','closed'] as $s)<option value="{{ $s }}" @selected($inquiry->status===$s)>{{ ucfirst($s) }}</option>@endforeach</select></label><label class="block"><span class="mb-2 block text-xs uppercase tracking-wider">Admin notes</span><textarea name="admin_notes" rows="7" class="w-full border p-3">{{ $inquiry->admin_notes }}</textarea></label><button class="bg-black px-6 py-3 text-white">Save request</button></form></div>@endsection
