@extends('admin.layouts.app')
@section('title','Payments') @section('header','Payment Methods')
@section('content')
@if(session('success'))<div class="mb-5 border border-emerald-200 bg-emerald-50 p-4 text-sm">{{ session('success') }}</div>@endif
<div class="space-y-4">@foreach($methods as $m)<form method="POST" action="{{ route('admin.payments.update',$m) }}" class="border border-black/10 bg-white p-5">@csrf @method('PUT')<div class="flex flex-wrap items-center justify-between gap-4"><div><b>{{ $m->name }}</b><p class="text-xs text-black/45">{{ $m->code }}</p></div><label class="text-sm"><input type="checkbox" name="enabled" value="1" @checked($m->enabled)> Enabled at checkout</label></div>@if($m->code==='bank_transfer')<div class="mt-5 grid gap-3 border-t border-black/10 pt-5 md:grid-cols-2"><input name="bank_name" value="{{ old('bank_name',$m->config['bank_name'] ?? '') }}" placeholder="Bank name" class="border border-black/15 p-3"><input name="account_title" value="{{ old('account_title',$m->config['account_title'] ?? '') }}" placeholder="Account title" class="border border-black/15 p-3"><input name="account_number" value="{{ old('account_number',$m->config['account_number'] ?? '') }}" placeholder="Account number" class="border border-black/15 p-3"><input name="iban" value="{{ old('iban',$m->config['iban'] ?? '') }}" placeholder="IBAN" class="border border-black/15 p-3"><textarea name="instructions" rows="3" placeholder="Customer payment instructions" class="border border-black/15 p-3 md:col-span-2">{{ old('instructions',$m->config['instructions'] ?? '') }}</textarea></div>@endif<button class="mt-5 bg-black px-5 py-3 text-xs uppercase tracking-[.14em] text-white">Save payment method</button></form>@endforeach</div>
@endsection
