@extends('admin.layouts.app')
@section('title','Pages') @section('header','Pages') @section('eyebrow','CMS / pages')
@section('content')
<div style="display:flex;justify-content:space-between;align-items:flex-end;gap:14px;flex-wrap:wrap;margin-bottom:18px"><div><h2 style="font-size:27px;margin:0">Content pages</h2><p class="admin-muted" style="font-size:12px;margin:7px 0 0">Editorial, service and legal pages with publishing and SEO controls.</p></div><a class="admin-btn admin-btn-primary" href="{{ route('admin.pages.create') }}">+ Create page</a></div>
<section class="admin-card"><form method="GET" style="display:flex;gap:8px;padding:14px;border-bottom:1px solid #e4e7ec"><input class="admin-field" name="q" value="{{ request('q') }}" placeholder="Search title or slug"><button class="admin-btn">Search</button></form><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Page</th><th>Template</th><th>Status</th><th>Published</th><th style="text-align:right">Actions</th></tr></thead><tbody>@forelse($pages as $p)<tr><td><a style="font-weight:700" href="{{ route('admin.pages.edit',$p) }}">{{ $p->title }}</a><div class="admin-muted" style="font-size:9px;margin-top:4px">/{{ $p->slug }}</div></td><td>{{ ucfirst($p->template) }}</td><td><span class="admin-status {{ $p->status==='published'?'success':'warning' }}">{{ ucfirst($p->status) }}</span></td><td>{{ optional($p->published_at)->format('d M Y H:i') ?: '—' }}</td><td style="text-align:right"><div style="display:flex;justify-content:flex-end;gap:6px"><a class="admin-btn" href="{{ route('admin.pages.edit',$p) }}">Edit</a><form method="POST" action="{{ route('admin.pages.duplicate',$p) }}">@csrf<button class="admin-btn">Duplicate</button></form></div></td></tr>@empty<tr><td colspan="5" class="admin-muted" style="padding:45px;text-align:center">No pages.</td></tr>@endforelse</tbody></table></div></section><div style="margin-top:18px">{{ $pages->links() }}</div>@endsection
