# Header Menu Scroll Lock Hotfix

- Locks the underlying page whenever the full-screen header mega menu is open.
- Stops Lenis smooth scrolling while the menu is open and resumes it when closed.
- Keeps the menu overlay itself independently scrollable.
- Escape key and menu links continue to close the menu and release the scroll lock.
- No visual redesign was applied.
