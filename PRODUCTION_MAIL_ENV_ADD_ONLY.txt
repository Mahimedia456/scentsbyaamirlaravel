# Your current production .env uses MAIL_MAILER=log, which does NOT deliver real emails.
# Change/add only these mail lines. Do not replace the rest of your .env.

MAIL_MAILER=smtp
MAIL_SCHEME=tls
MAIL_HOST=smtp.stackmail.com
MAIL_PORT=587
MAIL_USERNAME=orders@scentsbyaamir.com
MAIL_PASSWORD="<ACTUAL_STACKMAIL_MAILBOX_PASSWORD>"
ORDER_NOTIFICATION_EMAIL=orders@scentsbyaamir.com

# Existing values can stay:
# MAIL_FROM_ADDRESS=orders@scentsbyaamir.com
# MAIL_FROM_NAME="Scents by Aamir"

# After editing production .env:
# /usr/php84/usr/bin/php artisan optimize:clear
# /usr/php84/usr/bin/php artisan config:clear
# /usr/php84/usr/bin/php artisan storefront:mail-check --to=orders@scentsbyaamir.com
#
# Do NOT run config:cache for this project.
