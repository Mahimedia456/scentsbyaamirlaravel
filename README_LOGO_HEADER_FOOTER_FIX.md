# Header + Footer Logo Fix

- Header now uses `/public/logo-02.png`.
- Footer now uses `/public/logo.png`.
- Header is full width and always readable.
- Menu changed from cramped side drawer to a full-width mega menu below the header.
- Footer keeps a structured responsive layout with the actual logo asset.

If your files are exactly:
- `public/logo-02.png`
- `public/logo.png`

no further path changes are required.
